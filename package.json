{
  "name": "saigai_shikoku",
  "version": "0.1.0",
  "private": true,
  "description": "四国防災情報マップ - core logic tests",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "vitest": "^3.0.0"
  }
}
